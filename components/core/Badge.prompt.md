**Badge** — availability/status pill with an optional pulsing neon dot.

```jsx
<Badge live>Беру проекты в работу · отвечаю в течение часа</Badge>
<Badge>Next.js · без шаблонов</Badge>
```

`live` adds the pulsing accent dot (signals "online / open"). Mono, small radius, glass background.
