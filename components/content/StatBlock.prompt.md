**StatBlock** — a single headline metric (display number + mono label).

```jsx
<StatBlock value="5+" label="лет в коммерческом коде" />
<StatBlock value="5.0" mark="★" label="высший рейтинг на Kwork" />
```

`mark` (e.g. ★) renders in accent. Lay several in a row for the hero stat strip.
